$kind: http-request
url: "{{current_api_url}}/teacher/assignment-edit?id=4"
method: GET
headers:
  Accept: application/json
queryParams:
  id: "4"
auth:
  type: bearer
  credentials:
    token: "{{teacher_token}}"
order: 2000
